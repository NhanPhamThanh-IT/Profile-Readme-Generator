export const styles = (darkMode) => ({
    description: {
        marginBottom: 4,
        maxWidth: '800px',
        marginLeft: 'auto',
        marginRight: 'auto',
        color: darkMode ? '#e0f2f1' : '#004d40',
    },
});
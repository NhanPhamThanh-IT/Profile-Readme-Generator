export const styles = (darkMode) => ({
    title: {
        fontWeight: 700,
        marginBottom: 2,
        fontSize: { xs: '2rem', md: '3rem' },
        color: darkMode ? '#1de9b6' : '#004d40',
    },
});
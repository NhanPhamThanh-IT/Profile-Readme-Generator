/**
 * @file Header.js
 * @description Defines the navigation routes used in the Readme Generator application.
 */

/**
 * A list of pages used for application navigation.
 * Each page includes a display name and the corresponding path.
 *
 * @constant {Array<{name: string, path: string}>}
 */
export const pages = [
    { name: 'Home', path: '/' },
    { name: 'Generator', path: '/generator' },
];

/**
 * Import `FeatureCard` component from the current module.
 * 
 * @module FeatureCard
 * @example
 * import FeatureCard from './FeatureCard';
 * 
 * // Example usage of the FeatureCard component
 * const icon = SomeIcon; // Replace with an actual icon component
 * const title = "Feature Title";
 * const description = "This is a short description of the feature.";
 * const darkMode = true; // Set to false for light mode
 * 
 * return (
 *   <FeatureCard
 *     icon={icon}
 *     title={title}
 *     description={description}
 *     darkMode={darkMode}
 *   />
 * );
 */
import FeatureCard from './FeatureCard';

export default FeatureCard;
